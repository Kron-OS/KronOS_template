[Unit]
# The nested Docker daemon must not accept work (and its bridge must not
# come up) before the egress firewall has blocked the local network.
Requires=kronos-firewall.service
After=kronos-firewall.service
