apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "kronos.fullname" . }}-celery-plaso
  namespace: kronos
  labels:
    {{- include "kronos.labels" . | nindent 4 }}
    app.kubernetes.io/component: celery-plaso
spec:
  replicas: {{ .Values.celery.workers.plaso.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "kronos.name" . }}-celery-plaso
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "kronos.name" . }}-celery-plaso
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/component: celery-plaso
        app.kubernetes.io/zone: app
      annotations:
        {{- include "kronos.vaultAnnotations" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "kronos.serviceAccountName" . }}
      automountServiceAccountToken: false
      runtimeClassName: {{ .Values.firecrackerRuntimeClass }}
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: celery-plaso
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command:
            - celery
            - -A
            - src.external.celery_app
            - worker
            - -Q
            - {{ .Values.celery.workers.plaso.queues | quote }}
            - -c
            - {{ .Values.celery.workers.plaso.concurrency | quote }}
            - --loglevel=info
          env:
            {{- include "kronos.commonEnv" . | nindent 12 }}
          envFrom:
            - secretRef:
                name: kronos-app-secrets
          resources:
            {{- toYaml .Values.resources.celeryPlaso | nindent 12 }}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - name: tmpfs-scratch
              mountPath: /tmp
      volumes:
        - name: tmpfs-scratch
          emptyDir:
            medium: Memory
            sizeLimit: 4Gi
